version: 0.2
env:
  variables:
    key: "value"

phases:
  install:
    commands:
      - python setup.py -q install
  build:
    commands:
      - echo Build completed
  post_build:
    commands:
      - echo Post Build completed
      - echo "******** Building docker site ********"
      
